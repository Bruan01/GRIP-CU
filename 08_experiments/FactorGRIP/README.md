# FactorGRIP Experiment Registry

本目录保存从 RecurrentGRIP v1.1.1 diagnostic cross 结果派生出的新主线。每个版本独立留存，不修改 `13_base_method/grip-exp/`，也不覆盖 RecurrentGRIP 的任何版本。

| 版本 | 目标 | 状态 |
|---|---|---|
| `v0_1_candidate_energy_probe_2026-08-30` | 用现有 adapter 区分 decoder/retrieval failure 与 storage failure | 已设计，待实现和 WSL 运行 |
