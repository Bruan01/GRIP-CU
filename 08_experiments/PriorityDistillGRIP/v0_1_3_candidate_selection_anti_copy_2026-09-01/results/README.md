# Results

Runtime outputs are written under `results/runs/<RUN_ID>/`.

- Commit: metrics, predictions, audits, logs, and reports.
- Do not commit: model/adapter weights (`*.pt`, `*.safetensors`, etc.).
