# Results

WSL runs are written to `results/runs/<run_id>/<method>/seed_<seed>/`.
Commit metrics, summaries, reports, environment snapshots, and predictions when a run is analyzed. Adapter weights remain ignored unless explicitly needed for release.
